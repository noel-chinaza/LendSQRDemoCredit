node_modules
# Keep environment variables out of version control
.env
package-lock.json

# don't put dist files on github, only put on heroku
**/dist
# don't put migration files on github, only put on heroku
**/migrations
# **/coverage
